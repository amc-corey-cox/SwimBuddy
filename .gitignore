node_modules/
dist/
dist-ssr/
coverage/
*.local
.DS_Store
.vite/
*.log
screenshots/
playwright-report/
test-results/

# The LinkML toolchain: a Python venv, set up per schema/README.md. Never committed.
# No trailing slash — it is often a symlink to a venv kept outside the repo.
.linkml
